import { OpenDirectivesDirective } from './open-directives.directive';

describe('OpenDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
